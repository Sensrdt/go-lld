# OOP Principles in Go

This folder demonstrates how to apply Object-Oriented Programming (OOP) concepts in Go.

## File Implementations

- **`main.go`**: Contains examples of Encapsulation (using unexported struct fields with getter/setter methods), Polymorphism (via interfaces), and Composition (embedding structs to reuse fields and methods instead of classical inheritance).
